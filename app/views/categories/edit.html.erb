<h1>Edit Category <%= @category.name %></h1>

<%= simple_form_for @category, url: "/categories/update/#{@category.id}", method: "POST" do |f| %>
	<%= f.input :name, input_html: { class: "form-control" } %>
	<hr>
	<%= f.submit :save %>
<% end %>
